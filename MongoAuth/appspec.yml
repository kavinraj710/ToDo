version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/todo

permissions:
  - object: /home/ubuntu/todo
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: ubuntu
